# File: .gitattributes
# Policy:
# - Treat text as text automatically.
# - Check out LF for all text by default.
# - Only .bat/.cmd use CRLF (Windows-native).

* text=auto eol=lf

# Repo meta files: keep LF (prevents “mixed” popups)
.gitattributes text eol=lf
.editorconfig  text eol=lf
.gitignore     text eol=lf

# Windows-native scripts that actually want CRLF
*.bat text eol=crlf
*.cmd text eol=crlf

# Binaries (never touch line endings)
*.png  binary
*.jpg  binary
*.jpeg binary
*.gif  binary
*.ico  binary
*.zip  binary
*.7z   binary
*.dll  binary
*.exe  binary
*.pdb  binary
*.pdn  binary

# SVG is text (useful diffs); keep LF
*.svg  text eol=lf
